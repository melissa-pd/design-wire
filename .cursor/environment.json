{
  "name": "The Design Wire",
  "install": "python3 --version\npython3 -c \"import ast, pathlib; ast.parse(pathlib.Path('add_edition.py').read_text()); print('add_edition.py: syntax ok')\"",
  "terminals": [
    {
      "name": "web",
      "command": "python3 -m http.server 8000",
      "description": "Serves the static site (index.html) at http://localhost:8000. Open this to preview The Design Wire."
    }
  ],
  "ports": [
    {
      "name": "web server",
      "port": 8000
    }
  ]
}
